// handleUserRegistration (if on start isUserRegistered == false)
// handleUserLanguageUpdate
// sendUserCreditCard
import TelegramBot from "node-telegram-bot-api";
export class UserController {
  private bot: TelegramBot;

  constructor(bot: TelegramBot) {
    this.bot = bot;
  }

//   public async handleUserRegistration(msg: TelegramBot.Message){

//     const langugageKeyboard
//   }
}
